<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.project.main.dao.ProjectInterface">

 	<!-- 로그인 처리  -->
	<select id="login" resultType="String">
		SELECT ID FROM JOIN_REG WHERE ID=#{param1} AND PW=#{param2}
	</select>
	
	<!-- 회원정보 수정 보기 -->
 	<select id="Mem_ModiView" resultType="com.project.main.dto.MemberInfo">
 		SELECT * FROM join_reg WHERE join_idx = #{param1}
 	</select>
 	
 	<!-- 회원 정보 수정 -->
 	<update id="Member_Modify">
 		UPDATE join_reg SET userPw = #{param2}, userName = #{param3}, userBirth = #{param4}, userEmail = #{param5} 
 			WHERE join_idx = #{param1}
 	</update>
 	
 	<!--패션토크 상세보기  -->
 	<select id="FT_Board_Detail" resultType="com.spring.main.dto.BoardDTO">
 		SELECT * FROM TOTAL_BOARD WHERE idx = #{param1}
 	</select>
 	
 	<!--패션토크 조회수 증가  -->
 	<update id="upHit">
 		UPDATE TOTAL_BOARD SET bHit = bHit+1 WHERE idx = #{param1}
 	</update>
 	
 	<!--코디게시판 상세보기  -->
 	<select id="CodiBoard_Detail" resultType="com.spring.main.dto.BoardDTO">
 		SELECT * FROM TOTAL_BOARD WHERE idx = #{param1}
 	</select>
 	
 	
</mapper>